{
  "name": "@gina-haya/shared",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "dev": "tsc --watch",
    "verify": "tsc && git diff --exit-code -- dist/"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
