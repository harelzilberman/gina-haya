[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@8.15.9", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @gina-haya/shared build",
  "pnpm --filter @gina-haya/api build"
]

[start]
cmd = "node packages/api/dist/index.js"
